# Support homework project

To run this project, download the files and run the following commands:

```
npm i
```

Once the npm packages have been installed, run this command to launch the project locally:

```
npm start
```
